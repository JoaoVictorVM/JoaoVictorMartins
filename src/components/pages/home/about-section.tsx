export default function AboutSection() {
    return (
        <section id="about" className="{aboutSection} h-screen bg-[var(--color-light)] flex items-center justify-center">
        </section>
    )
}